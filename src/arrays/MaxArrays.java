package arrays;import java.util.Scanner;public class MaxArrays {    public static void main(String[] args) {        Scanner scanner = new Scanner(System.in);        System.out.print("Введите длину массива ");        int maxLength = scanner.nextInt();        int[] numbers = new int[maxLength];        System.out.println("Заполните массив целыми числами через пробел: ");        for(int i = 0; i < maxLength; i++) {            numbers[i] = scanner.nextInt();        }        //0 1        //1 6        //2        int maxNumbers = numbers[0];        for(int i = 0; i < maxLength -1; i++ ) {            if(maxNumbers < numbers[i + 1]) {                maxNumbers = numbers[i +1];            }        }        System.out.println("Максимальное целое число = " + maxNumbers);    }}